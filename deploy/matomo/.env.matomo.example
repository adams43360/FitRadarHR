# Copier en .env dans /srv/matomo (jamais commité)
# Générer avec : python3 -c "import secrets; print(secrets.token_urlsafe(24))"

MATOMO_DB_NAME=matomo
MATOMO_DB_USER=matomo
MATOMO_DB_PASSWORD=<générer-un-mot-de-passe>
MATOMO_DB_ROOT_PASSWORD=<générer-un-mot-de-passe>
